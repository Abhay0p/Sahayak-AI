const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const caregiver = await prisma.profile.findFirst({ where: { role: 'caregiver' } }); const elderly = await prisma.profile.findFirst({ where: { role: 'elderly' } }); if (caregiver && elderly) { try { await prisma.caregiverAssignment.create({ data: { caregiverId: caregiver.id, elderlyId: elderly.id, permissions: JSON.stringify(['view', 'edit']) } }); console.log('Assigned', elderly.firstName, 'to', caregiver.firstName); } catch (e) { console.log('Assignment might already exist'); } } else { console.log('Missing caregiver or elderly'); } } main().catch(console.error).finally(() => prisma.$disconnect());
